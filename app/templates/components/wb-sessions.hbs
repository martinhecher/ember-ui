<div class="wb-sessions">
	<header>{{label}}</header>

	<main>
	{{#hk-sidebar files=files items=items as |item|}}
		{{dynamic-component type=item.type meta=item files=files}}
	{{/hk-sidebar}}
	</main>
</div>